import { HoverColorExempleDirective } from './hover-color-exemple.directive';

describe('HoverColorExempleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorExempleDirective();
    expect(directive).toBeTruthy();
  });
});
